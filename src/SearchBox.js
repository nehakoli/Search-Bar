import React from 'react';

const SearchBox = (props) => {
      return(
            <input type='search'
            className='search'
            placeholder={}
            onChange={}
            />
      )
} 

export default SearchBox;